<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat List</title>

    {{!-- Scripts and Styles repeated across all pages --}}
    {{!-- These scripts should go first --}}
    {{>Scripts}}
    {{>Styles}}

    {{!-- Styles specific to this view --}}
    <link rel="stylesheet" href="/css/chat_list.css">
    {{!-- Scripts specific to this view --}}
    <script src="/js/chat_list.js"></script>
</head>

<body>
    {{>Header title="Chat List" backPath="/home"}}
    <div id="scroll">
        {{#each chats}}
        <div class="chatRow">
            <button class="optionsButton" onclick="openOptions('{{this.chat_id}}', '{{this.chatname}}')">⋮</button>
            <span class="chatName" onclick="openChat('{{this.chat_id}}')">{{this.chatname}}</span>
            <span> | </span>
            <span class="chatDate">{{this.count}} users</span>
        </div>
        {{/each}}
    </div>
</body>

</html>